# 交互方式优化 - 从拖拽改为右键菜单

## 问题
用户反馈拖拽功能不稳定，任务无法拖入任务集

## 解决方案
改用右键菜单的方式添加任务到任务集

## 新的交互流程

### 1. 创建任务集
- 在科研/生活等窗口（非待办窗口）右键空白处
- 选择"创建任务集"
- 输入任务集名称，保存

### 2. 添加任务到任务集
- 右键点击任务集
- 选择"添加任务到此任务集"
- 弹出任务选择对话框
- 显示当前窗口下所有可添加的任务（未完成、非任务集、未归属其他任务集）
- 点击任务即可添加到任务集

### 3. 编辑子任务
- 右键点击子任务
- 选择"编辑任务"
- 修改优先级、开始日期、结束日期

## 实现细节

### 新增功能
1. **openAddTaskToGroup(groupId)** - 打开任务选择对话框
2. **addTaskToGroup(taskId, groupId)** - 将任务添加到任务集
3. **任务选择模态框** - 显示可添加的任务列表

### UI 改进
- 任务列表带优先级颜色点
  - 红色：重要
  - 橙色：通知
  - 蓝色：普通
  - 灰色：低优先级
- 悬停效果：高亮和轻微位移
- 空状态提示："当前窗口没有可添加的任务"

### CSS 样式
```css
.task-select-list - 任务列表容器
.task-select-item - 单个任务项
.priority-dot - 优先级颜色点
```

## 优势
1. **更可靠** - 不依赖浏览器拖拽 API
2. **更直观** - 用户可以看到所有可添加的任务
3. **更灵活** - 可以添加过滤、搜索等功能
4. **更友好** - 清晰的视觉反馈

## 测试步骤
1. 在科研窗口创建任务集"论文阅读"
2. 在科研窗口创建几个普通任务
3. 右键点击"论文阅读"任务集
4. 选择"添加任务到此任务集"
5. 在弹出的对话框中点击任务
6. 确认任务成功添加到任务集中

## 版本信息
- 优化版本：0.1.0
- 优化日期：2026-05-24
- 改进类型：交互方式优化
