# 变更记录

## 2026-09-13 — 计时浮窗、互斥面板与可恢复回收站

- 修复 Tauri 定向事件与全局监听不匹配；浮窗明确发送暂停/继续状态，附带计时会话与请求编号，拒绝迟到的旧会话命令。按钮及其 SVG 不再触发原生拖动，无响应时给出可见反馈。
- 计时浮窗跟随主题；主窗口底部和正在计时的任务行增加可见暂停/继续控制，不再依赖右键。
- 日历、搜索、设置、日记统一为互斥面板，重复点击/Escape关闭。改用可伸缩工作区，去除搜索栏绝对定位；窄窗口工具栏换行，面板不遮住计时控件，日记切换保留草稿。
- “完成”入口改为回收站，分已完成/已删除；提供文字撤销、目标日期选择和完成/删除后的撤销提示。删除保留任务、说明、计时与同批次子任务，重启和备份不丢失；恢复不复活旧提醒，需重新设置。
- 服务端过滤已删除任务的 bot 视图、拒绝对其完成/设置提醒并取消未发送提醒。严格校验的旧客户端不理解新删除字段，测试 API 与 EXE 应配套升级。
- 加入严格 Tauri 路由单测、软删除/提醒/双客户端/轮询回归，以及真实 Chromium 工具布局与操作脚本 `npm run test:ui`；CI 仅上传合成截图。
- 本机 41 项前端、55 项 API/桌面数据层、27 项插件、7 项工具测试，三套类型检查、Tauri 版本检查和构建通过；Node 与 Docker/Chromium 端到端、新 UI Docker 回归均通过。
- 同步机制仍是本机立即提交、其他端约 5 秒轮询，不宣称 WebSocket 实时；原生 Windows 与真实 QQ 仍需验收。本轮不导入原始个人数据，详见[独立测试清单](docs/UI_ACCEPTANCE.md)。

## 2026-09-13 — Windows 构建依赖对齐

- GitHub 首轮功能分支 CI 发现通知插件 Rust `2.4.0` 与 npm `2.3.3` 不匹配；将 npm 通知插件对齐到 `2.4.0`，不跳过 Tauri 的版本检查。
- 增加 `npm run check:tauri-versions`，直接检查两份锁文件中的 Tauri API/插件 major.minor，提前发现跨语言依赖漂移；附带工具回归测试。
- 修复提交 `471b945` 的 [GitHub Actions](https://github.com/zyhzxyz/todo-widget/actions/runs/34739378441) 已全部通过：前端21项、API/桌面49项、插件27项、工具7项、类型检查、构建、Node及Docker/Chromium端到端、Windows EXE构建。
- 已下载并核对该次 Windows x64 EXE、插件 ZIP 的来源与结构，插件7文件逐一匹配提交内容；增加正确选择分支和解开 Actions 外层 ZIP 的说明。这不是 Windows 真机运行或真实 QQ 收发验收。

## 2026-09-13 — 部署与隔离端到端验证

- 增加 Node24 多阶段 Docker 镜像与独立 Compose 服务：Linux host 网络/loopback，非 root UID1000，只读根文件系统，业务数据库使用私有独立卷。
- 提供互相独立的随机 app/bot token 生成工具（0600、拒绝覆盖、不打印密钥）、HTTPS/Caddy 示例，以及 Windows 显式迁移指南。
- 补齐 SQLite 在线备份、停服恢复、旧 DB/WAL/SHM 保留与权限说明；恢复失败不会继续启动服务。
- 增加使用真实 HTTP/SQLite、插件核心及模拟 OneBot 的端到端脚本；Docker 路径直接使用本仓库 Compose 并隔离项目/端口/数据卷，Chromium 验证原本地导出、远程每日目标与断网只读。
- 验证丢失写入响应、QQ失败退避、完成后取消、ACK丢失后重启、在线备份、实际恢复及幂等账本；所有测试只使用合成数据，不调用真实QQ。
- 本机前端21项、API/桌面49项、插件27项、三套类型检查、前端构建、Node和Compose/Chromium端到端全部通过；`npm audit` 为0漏洞。
- CI 已配置以上检查、插件 ZIP 与 Windows EXE 构建。GitHub 授权已完成，基础修复 `faa4b18` 已推送 `main`，功能实现 `d1107c4` 已推送 `feature/server-astrbot`；修复后的远程 CI 已通过，见上方对应提交的验证记录。生产部署和真实 Windows/QQ 验收仍待完成，不把本机模拟结果当作真机上线结果。

## 2026-09-13 — AstrBot 插件

- 新增可单独打包的 AstrBot 插件：QQ 私聊白名单、真实事件绑定、确定性命令与4个自然语言工具。
- API 确认后才报告成功；写请求先落盘，原消息/同操作安全重试，409不覆盖。
- 持久化回执、单条租约、发送前验证、非零 OneBot message_id ACK；用 text segment 防止 CQ 码注入。
- 支持 Docker 专用数据目录、最小权限与防重复进程锁；默认禁用，不触碰现有机器人。
- 27项插件单元测试、49项API/桌面测试通过；实际 AstrBot 框架隔离导入和工具注册通过。尚未进行真实 QQ 发信。

## 2026-09-13 — 服务器功能分支（桌面接入）

- 增加独立本地/服务器数据层、只读缓存、写入前日志、revision 冲突保护和同编号重试。
- 设置增加 JSON 导出、原始存储救援、空服务器导入预检及 QQ 绑定/提醒状态。
- 新增每日目标入口与提醒时间编辑；账户业务日期使用服务器时区。
- 损坏 JSON（包括已有 `null`/空字符串）不再用示例数据覆盖；损坏的待保存日志必须明确处理。
- 计时每 5 秒检查点及停止时保存恢复副本，稳定条目 ID 防止重复补记；允许明确放弃孤立恢复副本。
- 浏览器预览不再因缺少 Tauri API 白屏；文件导出支持浏览器下载和 Tauri 保存对话框。
- 21 项前端测试、47 项桌面/API 测试、类型检查和前端构建通过；320×560 和 900×760 Chromium 预览通过。
- Rust lockfile 已更新；Windows 原生保存对话框、托盘和真实 QQ 尚未验收，不能以浏览器/mock 测试替代。

## 2026-09-13 — 本地版可靠性修复

- 修复北京时间凌晨“今天”落到前一天，以及完成热力图整月偏移问题；计时统计也使用本地日历日期。
- 修复从 31 日切换日历月份时跳过短月份的问题。
- 定时任务、每日目标及待办计数会在分钟切换时更新；窗口恢复焦点/可见时立即重新检查。
- 切换计时任务前先保存上一段；暂停后继续保留原始开始时间；忽略过期的异步窗口创建请求。
- 已保存的空任务数组不再自动恢复示例任务。
- 合并置顶状态的控制逻辑，护眼提示关闭后恢复用户偏好。
- 增加 15 项自动化回归测试和 GitHub Actions（前端检查 + Windows EXE 构建）。
- 更新锁定依赖，修复 `npm audit` 报出的开发工具依赖漏洞。

### 数据兼容与验证边界

- 不修改原有存储键，不删除或重置现有个人数据。
- 不批量平移历史 `completionDates`：旧记录无法可靠判断哪些日期受过 UTC 问题影响，自动挪动可能破坏正确记录。
- 本机验证了测试、前端构建和依赖审计。测试中的原生窗口接口使用模拟实现；Windows 构建结果以 GitHub Actions 为准，真实桌面操作仍需 Windows 验收。
- 此基础修复提交仅涉及本地存储版；服务器存储与 AstrBot 联动在单独功能分支推进。
