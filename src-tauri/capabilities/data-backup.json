{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "data-backup",
  "description": "Only the main window can save a JSON backup to a user-selected file.",
  "windows": ["main"],
  "permissions": ["dialog:allow-save", "fs:allow-write-text-file"]
}
